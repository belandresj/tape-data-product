# Phase 2 — One-second base implementation

Status: entry checkpoint specified, 2026-09-14; production replay and the full implementation specification remain pending. This document fixes the next job's deliverable without pretending the integrated calculation design has already been reviewed.

Update, 2026-09-14: the user requested one detailed implementation handoff spanning base, default features, bounded local verification and manual full-job preparation. Use [the combined implementation specification](feature_pipeline_implementation.md) for that task. It supplies the detailed design; production builders and committed executable fixtures/oracles remain pending. Preserve the Phase 2 base-acceptance checkpoint before Phase 3 integration. The original checkpoint-A-only stopping rule below applies only when a task requests that narrower scope.

## Scope and dependencies

Implement raw trades/quotes to `tape_base_1s_v1` under accepted Phase 0 schemas, timing, eligibility, numerical precision, freshness, coverage and interruption rules. Full raw staging is operationally useful, not a dependency for synthetic correctness work. Real-data measurements require verified local inputs and an explicit bounded scope. Read `planning/rewrite/raw_migration_completion.md` if it exists; its absence must not be interpreted as successful migration.

Product priorities and physical candidates are in [storage and migration](storage_and_migration.md). No feature EW/p90 calculation, corpus build, final serving-layout selection or legacy output relabeling belongs in Phase 2. Keep logical symbol-session builds independent from later multi-member packing. New half-life calculations remain Phase 3.

## Checkpoint A — Reviewed implementation spec and independent oracle

The first Phase 2 job must do concrete preparation and validation, not stop at another general plan:

1. Inspect the current integrated replay, storage, contract and test code. Record the actual baseline revision/local changes, reusable components and their ownership. Read Phase 0 completion/schema/robustness decisions; do not infer new semantics from legacy code.
2. Expand this file into the complete Phase 2 implementation specification required by the master plan: exact input admission, source clock/order, source-unit conversion, quote/trade state transitions, output fields and validity, public package/CLI interfaces, completion/restart manifests, algorithm complexity, buffering, disk and process-tree RSS bounds, baseline compatibility checks and acceptance criteria. Address historical evidence gaps explicitly; do not invent receipts.
3. Supply a field-by-field mapping from accepted base columns to inputs, accumulator/state, finalization rule and independent expected-value case. Cover every field, including continuity, halt and age lower-bound metadata.
4. Add small independently calculated raw fixtures and an expected-output oracle for timing endpoints, unequal quote durations, locked/crossed/recovered states, independent size validity, exact trade shares/dollars, inclusive one-second report-age cutoff, known zero versus missing observations, event age origins, source gaps, halts and session seeding. Expected results must not be generated by the production replay code being tested. Validate the fixtures against installed contracts where possible; do not add placeholder passing tests implying a builder exists.
5. Review spec/oracle independently as required by the master phase plan; resolve findings. Keep any external job ownership exclusive. Specify the next bounded implementation checkpoint so a subsequent agent can implement without inventing mathematical rules.

Checkpoint A can inspect schema/footer metadata of the verified local sample to assess source admission. Do not start a full raw decode/build or broaden external sampling as part of specification preparation. Define exact local sample members, session-start duration, bytes/read limits and guard settings for a later measured production run.

Acceptance: reviewed complete spec, independently checked fixture/oracle evidence, explicit source-admission gaps, preserved legacy checks, and a bounded implementable next checkpoint. Report spec/fixture completion separately from production calculator completion. Stop here if this is the requested first checkpoint; proceed to implementation only when the task explicitly includes it.

## Remaining Phase 2 completion requirements

Implement shared quote/trade event processing through the installed package. Produce the complete accepted base in bounded passes with no unnecessary trade–quote join. Verify exact values and null/reason behavior against the independent oracle; exercise batch-size invariance, member restart/reuse and boundary behavior. Demonstrate a small additional accumulator without replacing the replay machinery.

Measure the exact production path on an explicitly bounded, admitted external sample with process-tree RSS, rows, bytes, time, disk high-water and failure accounting. Report which members cannot be admitted and why. Update implemented documentation and a concise Phase 2 completion record only for behavior actually delivered. Full-corpus calculation remains behind the Phase 5 measurement and confirmation checkpoint.
